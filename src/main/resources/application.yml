spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/book_eat_nepal
    username: postgres
    password: 6441
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
    show-sql: false
    open-in-view: false
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
server:
  servlet:
    context-path: /book-eat-nepal/api/v1
  port: 8080
bandobasta:
  app:
    jwtSecret: ======================Bandbobasta=Spring===========================
    jwtExpirationMs: 86400000
    jwtCookieName: bandobasta
